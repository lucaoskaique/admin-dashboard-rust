{
  "name": "workflow-dashboard",
  "version": "1.0.0",
  "description": "A modern workflow visualization dashboard built with Yew and Rust/WASM. Features D3-based flow rendering, ELK graph layout, and responsive UI.",
  "main": "tailwind.config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:css": "tailwindcss -i ./input.css -o ./dist/tailwind.css --minify",
    "watch:css": "tailwindcss -i ./input.css -o ./dist/tailwind.css --watch"
  },
  "keywords": ["rust", "wasm", "yew", "workflow", "visualization", "d3", "elk", "dashboard"],
  "author": "lucaoskaique",
  "license": "MIT",
  "type": "commonjs",
  "dependencies": {
    "elkjs": "^0.9.3"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.23",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.19"
  }
}
